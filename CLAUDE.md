# CLAUDE.md

请用中文回答用户的问题。

## 项目身份

**AI4SE 期末项目 · A 类 · 编程智能体（Coding Agent）Harness**

从零构建一个编程智能体 Harness——即将 LLM 转变为可靠、可治理的编程智能体的工程层。核心公式是 **智能体 = LLM + Harness**。LLM 只是决定下一步动作的"CPU"；Harness 则是其余的一切：决策循环、工具分发、护栏机制、反馈回路、记忆系统以及配置管理。

本项目使用 **Superpowers**（方法论框架）构建，并以 **Claude Code** 作为开发过程中的主要编程智能体。这其中的讽刺意味是刻意的：用一种 Harness 来构建另一种 Harness，然后从亲身经历出发批判这套方法论。

## 核心原则（不可妥协）

1. **机制必须通过代码实现，而非提示词。** 护栏是一个 `guardrail(action)` 函数，以确定性方式拦截危险命令——而不是在系统提示词里说"请不要删除数据库"。反馈信号是通过验证器/传感器解析输出并做出客观判断——而不是"让 LLM 自己检查自己的工作"。

2. **一切必须在没有真实 LLM 的情况下可测试。** 试金石：将 LLM 替换为 mock/stub 后，每个核心机制（工具分发、护栏拦截、反馈注入、记忆读写、停止决策）都必须能通过确定性单元测试来验证。如果某个"机制"在移除 LLM 后就消失了，那它从来就不是机制——它只是提示词。

3. **自己动手构建 Harness。** 智能体主循环、工具分发、护栏和反馈回路必须是我们的自主代码。可以使用底层构建块（LLM 提供商的 chat-completion API、HTTP 库、向量数据库、解析器），但**禁止**使用高层智能体框架（LangChain AgentExecutor、AutoGen、CrewAI、LlamaIndex agents，或任何编程智能体 SDK 内置的 agent runner）。

4. **TDD 是强制要求。** 红 → 绿 → 重构。在有失败的测试之前，不允许编写实现代码。由 Superpowers 的 `test-driven-development` 技能强制执行。

5. **代码、配置或 git 历史中不得包含凭据。** 密钥必须使用操作系统密钥链或带主密码的加密存储。首次运行必须引导用户安全地输入凭据。查看状态时绝对不输出明文。

## 架构：六大维度

所有六个维度必须有**最小可运行实现**。其中一个维度必须**深入**。

| 维度 | 最小实现 | 建议深入方向 |
|------|---------|-------------|
| **决策循环** | 上下文组装 → LLM 调用 → 解析动作 → 分发 → 反馈 → 停止检查 | — |
| **动作/工具** | 文件读写、执行 shell、运行构建与测试 | 带模式验证的工具分发 |
| **上下文与记忆** | 跨会话存储、按相关性检索 | 自定义存储与检索（非框架内置） |
| **治理/护栏** | 拦截危险操作、人机交互（HITL）暂停 | **推荐：** 护栏状态机、沙箱、作用域围栏 |
| **反馈回路** | 运行测试/代码检查、解析结果、反馈给循环 | **推荐：** 确定性验证器 + 失败分类 + 多轮自我修正 |
| **配置** | 约束智能体行为的声明式规则 | — |

**推荐的深入维度：** 治理（护栏/沙箱/HITL）或反馈（确定性验证器 + 多轮自我修正）。这两个维度天然是代码密集型的，最能体现工程深度。

## 技术约束

### 必须自己实现
- 智能体主循环（上下文 → LLM → 解析 → 分发 → 反馈 → 停止）
- 可注入 mock 的 LLM 抽象层
- 所有治理/护栏机制
- 所有反馈/验证机制
- 记忆存储和检索（如果选为深入维度）

### 允许使用（构建块）
- LLM 提供商 API（Anthropic、OpenAI 等）用于单轮 chat completion
- HTTP 库、向量数据库、解析器
- 操作系统密钥链库用于凭据存储
- 标准库和生态包

### 禁止使用
- 高层智能体框架（LangChain AgentExecutor、AutoGen、CrewAI 等）
- 编程智能体 SDK 内置的 agent runner
- 框架内置的记忆/治理作为我们产品的机制

### 技术栈（待 SPEC 中确定）
- 语言：TypeScript、Go、Rust 或 Python（在 SPEC 中选择并附理由）
- LLM 提供商：不限（在 SPEC 中记录并附理由）
- 分发方式：Docker/OCI、原生二进制或包管理器

## 交付物清单

- [ ] `SPEC.md` — 10 个标准章节 + "领域与机制设计"章节
- [ ] `PLAN.md` — 细粒度任务列表，含依赖关系、文件路径、验证步骤
- [ ] `SPEC_PROCESS.md` — 头脑风暴过程，≥3 轮迭代，冷启动验证结果
- [ ] 完整源代码 — Harness 内核 + mock-LLM 单元测试 + 机制演示
- [ ] 分发构件 — `Dockerfile` 或二进制构建脚本
- [ ] `README.md` — 安装、运行、分发、密钥安全设置、已知局限
- [ ] `AGENT_LOG.md` — 带时间戳的实现日志
- [ ] `.gitlab-ci.yml` — 含 `unit-test` 作业的 CI（最后一次运行必须为绿色）
- [ ] `REFLECTION.md` — 1500–2500 字反思（必须学生自写，如注明则允许 AI 润色）
- [ ] 可访问的线上部署 URL 及 WebUI
- [ ] 机制演示（3 个场景，使用 mock LLM）：
  1. 护栏拦截危险操作
  2. 反馈回路接收失败信息并改变下一步动作
  3. 来自所选深入维度的一项确定性行为

## 工作流程

### 阶段一：规格与计划（当前阶段）
1. 运行 `brainstorming` 技能 → 产出 SPEC.md
2. 运行 `writing-plans` 技能 → 产出 PLAN.md
3. 在 SPEC_PROCESS.md 中记录过程
4. 冷启动验证：将 SPEC + PLAN 交给**不同**的智能体类型、全新会话、无前置上下文，实现 1–2 个任务 → 记录差距 → 修订 SPEC/PLAN

### 阶段二：实现
1. 每个主要功能/模块使用 Git worktree
2. 每个任务使用一个子智能体（全新子智能体，单一职责）
3. TDD：先写失败的测试 → 子智能体实现 → 变绿 → 重构
4. 每个任务两阶段审查：符合规格 → 代码质量
5. 严重问题必须在下一个任务开始前修复
6. 每个任务完成后在 PLAN.md 中更新完成状态和 commit hash
7. 在 AGENT_LOG.md 中记录一切

### 阶段三：交付
1. 运行 `finishing-a-development-branch` 技能
2. 最终审查
3. 确保 CI 为绿色
4. 在干净机器上验证分发是否正常工作
5. 撰写 REFLECTION.md（学生自写）

## 代码规范

- **语言：** 待 SPEC 确定——选择一种并坚持使用
- **测试：** 所有 Harness 机制都必须有确定性单元测试；全局使用 mock LLM；测试命令必须是单次调用（`make test` 或等效形式）
- **提交风格：** 每次提交标注产生该提交的子智能体；在 commit body 中注明人工修改
- **PR 工作流：** 每个 worktree → 一个 PR；禁止单体提交
- **分支命名：** `feature/<name>`、`fix/<name>`，遵循 PLAN.md 任务中的模式
- **注释：** 在文件/函数的顶部用注释标注任何手写的核心代码

## 目录结构（建议）

```
/
├── docs/                    # 项目需求（仅供查阅）
├── spec/                    # SPEC.md、PLAN.md、SPEC_PROCESS.md
├── src/                     # Harness 内核源码
│   ├── loop/                # 智能体主循环
│   ├── tools/               # 工具定义与分发
│   ├── guardrails/          # 治理、HITL、沙箱
│   ├── feedback/            # 验证器、传感器、结果解析
│   ├── memory/              # 上下文存储与检索
│   ├── config/              # 配置加载与验证
│   └── llm/                 # LLM 抽象层 + mock
├── tests/                   # 单元测试（基于 mock LLM）
├── demo/                    # 机制演示脚本
├── ci/                      # CI 配置
├── README.md
├── AGENT_LOG.md
├── REFLECTION.md
└── Dockerfile               # 或等效分发构件
```

## 给 AI 助手的上下文

在协助本项目时，你正在帮助构建一个**编程智能体 Harness**——而不是配置一个已有的。每次写代码时：

1. 问自己："这个能用 mock LLM 测试吗？"如果不能，那它就不是机制，而是提示词。
2. 问自己："这是我们的代码，还是我们在委托给框架？"如果是委托，则不算数。
3. 遵循 TDD：没有失败的测试就先别写实现。
4. 绝不硬编码凭据或 API 密钥。
5. 有疑问时，重新阅读 `docs/` 中的两份文档——它们是根本依据。